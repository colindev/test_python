from setuptools import setup, find_packages

setup(
    name='tester',
    version='0.0.0',
    packages=find_packages(),

    classifiers = [
        'Programming Language :: Python :: 2.7'
    ],
)
